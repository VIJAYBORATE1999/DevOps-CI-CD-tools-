# Use a base image (adjust according to your backend requirements)
FROM ubuntu:latest

# Create a directory to store backend files
WORKDIR /app

# Copy backend files from the context to the /app directory
COPY . .

# Your backend application setup and commands go here

# Example: CMD ["./your-backend-app"]
